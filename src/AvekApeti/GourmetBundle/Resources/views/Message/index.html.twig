{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Message list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Item</th>
                <th>Content</th>
                <th>Datecreated</th>
                <th>Acclecture</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gourmet_message_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.item }}</td>
                <td>{{ entity.content }}</td>
                <td>{% if entity.dateCreated %}{{ entity.dateCreated|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.accLecture }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gourmet_message_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <table class="records_list">
        <thead>
        <tr>
            <th>Id</th>
            <th>Item</th>
            <th>Content</th>
            <th>Datecreated</th>
            <th>Acclecture</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity2 in entities2 %}
            <tr>
                <td><a href="{{ path('gourmet_message_show', { 'id': entity2.id }) }}">{{ entity2.id }}</a></td>
                <td>{{ entity2.item }}</td>
                <td>{{ entity2.content }}</td>
                <td>{% if entity2.dateCreated %}{{ entity2.dateCreated|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity2.accLecture }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('gourmet_message_show', { 'id': entity2.id }) }}">show</a>
                        </li>

                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        <ul>
        <li>
            <a href="{{ path('gourmet_message_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
